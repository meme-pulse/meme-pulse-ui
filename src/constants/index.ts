import { ChainId } from '@/lib/sdk';

export const DEFAULT_CHAINID = Number(import.meta.env.VITE_DEFAULT_CHAIN_ID) as ChainId;

export const STACK_SEASON_TIMESTAMP = 1754524800; // 2025 08 07 00:00:00 GMT
